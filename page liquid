{% comment %}
  Plantilla para las páginas estándar.
{% endcomment %}

{% layout 'theme' %}

{% section 'header' %}

<div class="template-page">
  <h1 class="page-title">{{ page.title }}</h1>
  <div class="page-content">
    {{ page.content }}
  </div>
</div>

{% section 'footer' %}
